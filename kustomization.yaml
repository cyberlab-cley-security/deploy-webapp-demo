apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# Reference the deployment manifest
resources:
  - deployment.yaml
# Dynamically set the Docker image name and tag
images:
  - name: IMAGE_PLACEHOLDER # Matches placeholder in deployment.yaml
    newName: "016246143337.dkr.ecr.us-east-1.amazonaws.com/fork-chrisley75-ecr-3d8" # Can be changed dynamically
    newTag: "998" # Can be updated via CI/CD
